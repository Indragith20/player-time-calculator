export interface IPlayer {
    playerName: string;
    jerseyNumber: string;
    minutes: number;
    seconds: number;
    timer: boolean;
}

export interface IExcelData {
    PlayerId: string;
    PlayerName: string;
    JerseyNumber: string;
    Minutes: number;
    Seconds: number;
}
